NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional LLM for the knowledge chat (OpenAI-compatible). If unset, the
# assistant still streams Thinking / Using tools and retrieves from LCDS.
LLM_API_KEY=
LLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

